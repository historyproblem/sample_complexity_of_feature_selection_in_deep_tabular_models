defaults:
  - experiment: resnet50_gumbel_adaptive_lambda_recovery_v1
  - tuning: resnet50_gumbel_adaptive_lambda_recovery_v1_adamw_lambda_grid_ordered
  - override /optimizer: adamw
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
