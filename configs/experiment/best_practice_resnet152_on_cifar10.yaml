# @package _global_

defaults:
  - /data: cifar10_best_practice
  - /model: resnet152
  - /method: plain
  - /train: resnet50_best_practice
  - /optimizer: adamw
  - /scheduler: cosine_200
  - /metrics: classification
  - /run_history: valid_accuracy_max
  - /tracking: default
  - _self_

device: "cuda:0"

model:
  lambda_coef: 0.0

mlflow:
  enabled: true
  experiment_name: feature_selection_experiment
  run_name: "best_practice_resnet152_on_cifar10"
  tags:
    model_type: "ResNet152"
    method: "plain"
    dataset: "cifar10"
    recipe: "best_practice_resnet152_on_cifar10"
