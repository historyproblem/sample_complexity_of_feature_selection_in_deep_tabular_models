# @package _global_

defaults:
  - /data: tinyimagenet200_best_practice
  - /model: resnet101_tinyimagenet200
  - /method: plain
  - /train: resnet50_best_practice
  - /optimizer: adamw
  - /scheduler: cosine_200
  - /metrics: classification
  - /run_history: valid_accuracy_max
  - /tracking: default
  - _self_

device: "cuda:0"

training_arguments:
  num_epochs: 240

scheduler:
  T_max: 240

model:
  lambda_coef: 0.0

mlflow:
  enabled: true
  experiment_name: feature_selection_experiment
  run_name: "best_practice_resnet101_on_tinyimagenet200"
  tags:
    model_type: "ResNet101"
    method: "plain"
    dataset: "tinyimagenet200"
    recipe: "best_practice_resnet101_on_tinyimagenet200"
