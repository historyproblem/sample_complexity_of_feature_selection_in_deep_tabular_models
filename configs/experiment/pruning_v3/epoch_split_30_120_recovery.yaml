# @package _global_
defaults:
  - /experiment/pruning_v3/epoch_split_recovery_base
  - _self_

one_shot:
  search_epochs: 30
  final_epochs: 120
  search_scheduler_horizon_epochs: 30

accuracy_guided:
  stage_plan:
    - {id: shared_search, kind: search, epochs: 30, commit_allowed: false, selected_checkpoint: null, restart_policy: adamw_cosine_restart}
    - {id: export_only, kind: commit, epochs: 0, commit_allowed: true, selected_checkpoint: null, restart_policy: none}
    - {id: final_recovery, kind: recovery, epochs: 120, commit_allowed: false, selected_checkpoint: null, restart_policy: branch_specific_optimizer_scheduler_handoff}

training_arguments:
  adaptive_lambda:
    soft_drop: 0.001
    hard_drop: 0.002

mlflow:
  run_name: epoch_split_30_120_recovery

run_history:
  run_name: epoch_split_30_120_recovery
