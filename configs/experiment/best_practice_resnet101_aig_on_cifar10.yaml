# @package _global_

defaults:
  - /data: cifar10_best_practice
  - /model: resnet101
  - /method: aig
  - /train: resnet50_best_practice
  - /optimizer: adamw
  - /scheduler: cosine_200
  - /metrics: aig
  - /run_history: valid_accuracy_max
  - /tracking: default
  - _self_

device: "cuda:0"

model:
  lambda_coef: 20.0
  backbone:
    resnet_block:
      temperature: 1.0

mlflow:
  enabled: true
  experiment_name: feature_selection_experiment
  run_name: "best_practice_resnet101_aig_on_cifar10_lambda_${model.lambda_coef}"
  tags:
    model_type: "ResNet101_AIG"
    method: "aig"
    dataset: "cifar10"
    recipe: "best_practice_resnet101_aig_on_cifar10"
