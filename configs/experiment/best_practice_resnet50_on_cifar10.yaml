# @package _global_

defaults:
  - /data: cifar10_best_practice
  - /model: resnet50
  - /method: plain
  - /train: resnet50_best_practice
  - /optimizer: adamw
  - /scheduler: cosine_200
  - /metrics: classification
  - /run_history: valid_accuracy_max
  - /tracking: default
  - _self_

device: "cuda:0"

model:
  lambda_coef: 0.0

mlflow:
  enabled: True
  experiment_name: feature_selection_experiment
  run_name: "adaptive_lambda_baseline"
  tags:
    model_type: "ResNet50"
    method: "plain"
    recipe: "adaptive_lambda_baseline"
