# @package _global_
#
# "Our approach" — single-shot (non-iterative) block/layer-granularity AIG
# gating with entropy regularization, on ResNet101/CIFAR10. See
# ablation/ours_non_iterative/resnet50_cifar10.yaml for the full explanation.

defaults:
  - /data: cifar10_best_practice
  - /model: resnet101
  - /method: aig
  - /train: resnet50_best_practice
  - /optimizer: adamw
  - /scheduler: cosine_200
  - /metrics: full
  - /run_history: valid_accuracy_max
  - /tracking: default
  - _self_

device: "cuda:0"

model:
  lambda_coef: 0.0001
  bypass_on_zero_lambda: false
  entropy_regularization: plus_negative_entropy
  entropy_regularization_coef: 0.3
  backbone:
    resnet_block:
      temperature: 1.0
      gate_regularization: l1_probability

training_arguments:
  num_epochs: 200
  lambda_warmup:
    enabled: false
  adaptive_lambda:
    enabled: false
  batchnorm_recalibration:
    enabled: false

scheduler:
  T_max: 200

mlflow:
  enabled: true
  experiment_name: feature_selection_experiment
  run_name: "ablation_ours_non_iterative_resnet101_cifar10_lambda${model.lambda_coef}_beta${model.entropy_regularization_coef}"
  tags:
    model_type: "ResNet101_AIG"
    method: "ours_non_iterative"
    dataset: "cifar10"
    recipe: "ablation_ours_non_iterative_resnet101_cifar10"
    gate_regularization: "l1_probability"
    entropy_regularization: "plus_negative_entropy"
