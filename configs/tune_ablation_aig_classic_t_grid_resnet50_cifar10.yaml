defaults:
  - experiment: ablation/aig_classic/resnet50_cifar10
  - tuning: ablation_aig_classic_t_grid_resnet50_cifar10_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
