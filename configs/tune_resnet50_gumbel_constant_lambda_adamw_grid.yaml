defaults:
  - experiment: best_practice_resnet50_gumbel_on_cifar10
  - tuning: resnet50_gumbel_constant_lambda_adamw_grid_200ep_1em8_1em6_10_50_ordered
  - override /optimizer: adamw
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
