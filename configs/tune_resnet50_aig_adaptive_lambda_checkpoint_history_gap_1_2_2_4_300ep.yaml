defaults:
  - experiment: resnet50_aig_adaptive_lambda_v1
  - tuning: resnet50_aig_adaptive_lambda_checkpoint_history_gap_1_2_2_4_300ep_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
