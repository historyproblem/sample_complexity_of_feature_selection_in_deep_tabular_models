defaults:
  - experiment: efficientnetv2_s_aig_adaptive_lambda_init1em4_cifar10
  - tuning: efficientnetv2_s_aig_plus_neg_entropy_coef_lambda1em4_120ep_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
