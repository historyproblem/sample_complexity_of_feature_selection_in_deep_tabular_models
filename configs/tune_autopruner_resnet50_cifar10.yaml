defaults:
  - experiment: autopruner_resnet50_cifar10
  - tuning: autopruner_keep_ratio_grid
  - _self_

reporting:
  run_label_fields:
    keep_ratio: model.backbone.target_keep_ratio

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
