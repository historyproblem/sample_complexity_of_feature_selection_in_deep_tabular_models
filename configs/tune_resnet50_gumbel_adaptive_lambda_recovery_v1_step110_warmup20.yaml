defaults:
  - experiment: resnet50_gumbel_adaptive_lambda_recovery_v1
  - tuning: resnet50_gumbel_adaptive_lambda_recovery_v1_step110_warmup20_1em6_1em4_boost5_until50_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
