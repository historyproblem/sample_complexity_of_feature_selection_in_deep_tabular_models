defaults:
  - experiment: stochastic_depth_resnet50_cifar10
  - tuning: stochastic_depth_resnet50_pL_grid_03_05_06_07_08_09_10_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
