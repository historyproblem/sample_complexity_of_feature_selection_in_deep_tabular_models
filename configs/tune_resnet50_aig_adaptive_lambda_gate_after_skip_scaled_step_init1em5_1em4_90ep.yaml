defaults:
  - experiment: resnet50_aig_adaptive_lambda_gate_after_skip_v1
  - tuning: resnet50_aig_adaptive_lambda_gate_after_skip_scaled_step_init1em5_1em4_90ep_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
