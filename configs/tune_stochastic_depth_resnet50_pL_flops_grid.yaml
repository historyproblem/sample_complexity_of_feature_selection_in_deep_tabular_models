defaults:
  - experiment: stochastic_depth_resnet50_cifar10
  - tuning: stochastic_depth_resnet50_pL_flops_grid_dense_10_09_08_07_05_03_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
