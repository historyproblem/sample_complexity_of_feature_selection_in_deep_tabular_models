defaults:
  - experiment: resnet50_aig_adaptive_lambda_v1
  - tuning: resnet50_aig_plus_neg_entropy_coef_lambda1em4_120ep_repeats3_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
