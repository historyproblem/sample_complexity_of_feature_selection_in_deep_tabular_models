defaults:
  - experiment: resnet50_aig_adaptive_lambda_v1
  - tuning: resnet50_aig_adaptive_lambda_no_extra_init_grid_1em8_1em5_1em2_1_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
