defaults:
  - experiment: stochastic_depth_resnet50_cifar10
  - tuning: stochastic_depth_resnet50_pL_grid_025_035_05_065_08_10_repeats3_ordered
  - _self_

hydra:
  run:
    dir: ${tuning.output_dir}/${now:%Y%m%d_%H%M%S}_${tuning.study_name}
  output_subdir: .hydra
