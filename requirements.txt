# AutoPruner runs on a Volta V100 (sm_70). Keep the training image's CUDA
# 12.6-compatible stack instead of upgrading to newer default CUDA wheels.
torch==2.6.0
torchvision==0.21.0
hydra-core
omegaconf
tqdm
mlflow
protobuf>=3.20.3,<6
numpy
pandas
matplotlib
ipykernel
plotly
nbformat
optuna
